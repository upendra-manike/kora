api deleteUser {
  input {
    id: UUID
  }

  output Boolean

  handler {
    let user = UserRepo.findById(id)
    if (user == null) {
      throw Error("User not found")
    }

    // Delete user from MongoDB
    UserRepo.delete(id)
    
    return true
  }
}

