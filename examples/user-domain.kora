(*
 * Example: User Domain Module
 * 
 * This demonstrates how to define domain types in Kora.
 *)

module domain User {
  type User {
    id: UUID
    name: String
    email: Email
    createdAt: Date
  }

  type UserProfile {
    user: User
    bio: String?
    avatar: String?
  }
}

