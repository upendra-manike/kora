api listPosts {
  input {
    limit: Number?
    offset: Number?
  }

  output Post[]

  handler {
    let limit = limit || 10
    let offset = offset || 0
    return PostRepo.findAll(limit, offset)
  }
}


